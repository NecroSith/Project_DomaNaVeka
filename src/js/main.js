$(document).ready(function() {
    $('.projects__slider').slick({
        // dots: true,
        // infinite: true,
        // speed: 300,
        arrows: false,
        centerMode: false,
        slidesToShow: 3
    });

    $('.wecando__slider-one').slick({
        // dots: true,
        // infinite: true,
        // speed: 300,
        centerMode: false,
        slidesToShow: 3
    });

    $('.wecando__slider-two').slick({
        // dots: true,
        // infinite: true,
        // speed: 300,
        centerMode: false,
        slidesToShow: 3
    });
});